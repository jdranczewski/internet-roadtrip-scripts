import * as IRF from 'internet-roadtrip-framework'

export const maplibre = await IRF.modules.maplibre;

export const vcontainer = await IRF.vdom.container;
export const vmap = await IRF.vdom.map;
export const vodometer = await IRF.vdom.odometer;
